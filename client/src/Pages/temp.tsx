import * as React from 'react';
import { useState } from 'react';
import TreeContainer from '../components/Shapes/TreeContainer';
export interface ITempProps {
}

export default function Temp (props: ITempProps) {
  return (
  <div>
    <TreeContainer/>
  </div>
  );
}
